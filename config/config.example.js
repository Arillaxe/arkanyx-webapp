module.exports = {
  webpack: {
    host: 'host', // usually either localhost or ip address of the machine you are running it from, i.e. ip address of the devbox, e.g. 10.20.1.111
    port: 'port', // port on which you are running it, usually port that is assigned to your devbox, e.g. 30160
    publicHost: 'publicHost', // public domain name on which you are accessing app from browser without http prefix, ususally your devbox url, e.g. stanislav.tyshchuk.d.fireflylabs.co
  },
};
